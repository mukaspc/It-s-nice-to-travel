---
import Layout from '../layouts/Layout.astro';
import { getSupabaseConfig } from '../db/supabase.client';

// Pobierz konfigurację Supabase
const supabaseConfig = getSupabaseConfig();
---

<Layout title="Sign In - It's nice to travel">
  <div 
    id="login-page" 
    data-page-type="login"
    data-supabase-url={supabaseConfig?.supabaseUrl}
    data-supabase-key={supabaseConfig?.supabaseKey}
  ></div>
  
  <script src="../scripts/login.ts"></script>
</Layout> 